#!/bin/sh
#
# commit-msg hook: strip Claude Code attribution lines from commit messages.
# Wired up globally via core.hooksPath in git/gitconfig.symlink.

grep -iv \
  -e 'Co-Authored-By: Claude' \
  -e 'Generated with Claude Code' \
  "$1" > "$1.tmp"

mv "$1.tmp" "$1"

exit 0
